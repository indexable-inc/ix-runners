#!/usr/bin/env -S uv run --locked --script
# /// script
# requires-python = ">=3.13"
# dependencies = ["ix-sdk==0.7.1"]
# ///
"""Reconcile the self-hosted ix runner pool; see ix_runners.py."""

from ix_runners import main

if __name__ == "__main__":
    main()
